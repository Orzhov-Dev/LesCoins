import { CryptoFilterPipe } from './crypto-filter.pipe';

describe('CryptoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CryptoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});